# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Android
local.properties
*.apk
*.aab
*.keystore
!debug.keystore

# Kotlin/JS and Node
node_modules/

# Engine sources fetched by tooling/engine-build (never vendored)
tooling/engine-build/.sources/
tooling/engine-build/.toolchain/
tooling/engine-build/.build/

# Game data must never be committed
*.wad
*.WAD
# A local shelf staged for a LAN file server; the extensions above cover the data
# itself, this covers the manifest and notes that would otherwise be tracked beside it
slipgate-server/
*.iwad
*.pk3
*.deh

# Xcode
xcuserdata/
*.xcuserstate
*.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist

# IDE
.idea/
*.iml
*.ipr
*.iws
.kotlin/

# macOS
.DS_Store

# Logs and local scratch
*.log
*.hprof
